name: Monitor Changes

on:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3
      - run: echo "Repository Changed Successfully!"